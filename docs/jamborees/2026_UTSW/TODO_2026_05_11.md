# TODO — 2026-05-11

Divide-and-conquer list for an internal teammate (has repo + Synapse access). Master jamboree state in [`TODO.md`](TODO.md); per-issue context in [`issues/`](issues/); IDs per dataset in [`synapse_paths.tsv`](synapse_paths.tsv).

## Tasks

### Hon CM CRISPR pipeline GCP — QC review + Synapse re-mirror &nbsp;·&nbsp; **Low**
Once the in-flight GCP re-run of the upstream Nextflow CRISPR pipeline completes, do a QC pass (`pipeline_dashboard/dashboard.html`, `figures/`, `additional_qc/`) and re-mirror the canonical 3-folder bundle to Synapse. Subsumes the previous "complete Hon CM CRISPR bundle" item.

- Pipeline run: `datasets/Hon_WTC11-cardiomyocyte-differentiation_TF-Perturb-seq/4_run_CRISPR_pipeline.sh` (nextflow `-profile google`, `-resume`)
- Recent fix: bumped `PreprocessAnnData` machine type to `n2-highmem-32` (commit `04e42ce`) after `n2-highmem-16` couldn't fit retry #3 at 150 GB
- Hon CM e-distance is already complete + mirrored (`syn74897350`); cNMF is gated on this bundle landing

### Compare e-distance runs across overlapping datasets &nbsp;·&nbsp; **High**
Goal: figure out the p-value calibration issue on Huangfu DE+ESC. Cross-reference our runs on overlapping datasets. May need to bring Chikara in.

- Our runs: Hon CM `syn74897350` (calibrated ✅), Huangfu DE `syn74883327` (broken ⚠️), Huangfu ESC `syn74883475` (broken ⚠️), HTv2 cleanser_800 (no mirror yet, bit-perfect to Chikara's `syn74895081`)
- Sara's runs: TBD where they live on Synapse (Gersbach lab project space) — ask her
- Calibration writeup with current evidence + plots: [`issues/edistance-calibration/`](issues/edistance-calibration/)
- Comparison tooling: `scripts/validate_edistance_outputs.py` + the per-type pval breakdown in `scratch/2026_05_09_edistance_calibration/calibration_plots.py`
- Chikara loop-in: send the writeup + Sara comparison once we have it

### Inspect cNMF outputs and document issues for review &nbsp;·&nbsp; **High**
Structural + content review of the cNMF outputs we have, then a writeup for Alexandra (cNMF subject-matter expert) to discuss.

- Datasets: Huangfu DE [`syn74893844`](https://www.synapse.org/Synapse:syn74893844), Huangfu ESC [`syn74893846`](https://www.synapse.org/Synapse:syn74893846), HTv2 testbed (verify job 10577039 terminal state first). Hon CM + Gersbach Hep gated on external bundles, no action here.
- Inspection:
  - Run `scripts/validate_cnmf_outputs.py` for structural compliance against [`schemas/cnmf.json`](schemas/cnmf.json)
  - Eyeball top-20 genes per program (biologically sensible?), k-selection diagnostics (stability / error curve), usage distributions (degenerate programs?), gene-spectra-score NaNs, regulator associations in `Eval/<sel>_<dt>/`
- Writeup: new [`issues/cnmf-output-review.md`](issues/cnmf-output-review.md) mirroring the [`issues/edistance-calibration/`](issues/edistance-calibration/) format — one bullet per finding, link to specific files / Synapse IDs. **Dump everything** (don't pre-triage) since we'll need to discuss with Alexandra anyway.

### Decide which derived files we package per output type (CRISPR pipeline / cNMF / e-distance) &nbsp;·&nbsp; **High**
Resolves the deferred items in Step 4 of [`TODO.md`](TODO.md) ("Define the human-readable outputs"). Reference + per-dataset CRISPR summary already exist; cNMF and e-distance derivatives are still TBD.

- Candidate cNMF derivatives ([`TODO.md`](TODO.md) lines 65-67): cross-dataset program-similarity heatmap (cosine-sim of `gene_spectra_score` across all 5 datasets at each k), per-dataset top-20-genes-per-program TSV, per-dataset regulators-per-program TSV (from `Eval/<sel>_<dt>/<sel>_perturbation_association_results_*.txt`).
- Candidate e-distance derivatives ([`TODO.md`](TODO.md) lines 70-71): cross-dataset TFs-significant-by-FDR summary TSV (rolled up from `pval_edist_full.csv` — **calibration concern affects this**, see the e-distance comparison task), cross-dataset clustered heatmap of per-target energy distances (joins `target_by_target_matrix.csv` on shared targets).
- Schema + folder consequence: once decided, [`schemas/`](schemas/) likely needs updates and we'd add a `derived/` (or per-output) subdir alongside the existing comprehensive bundles.

**Status: in flight** by another agent — sync with them before doing anything in this space; fold their decisions into schemas + per-dataset READMEs once they hand off.

### Update schemas + docs + final jamboree onboarding &nbsp;·&nbsp; **Medium**
Follow-up to the derived-files decision — but also rolls up the remaining Step 7 items from [`TODO.md`](TODO.md) (Google Sheet, agent-ready docs) so we land everything together once the derived files are decided.

- [`schemas/`](schemas/) — extend `cnmf.json` / `energy_distance.json` (and add new schema files if any new output type) with the derived artifacts' columns + curation rules.
- Per-dataset READMEs under `datasets/<id>/<analysis>/README.md` — add links to the derivatives + a one-line description of each.
- Top-level [`TODO.md`](TODO.md) + [`README.md`](README.md) — flip Step 4 from deferred to complete; update the "Outputs to package for the jamboree" section.
- [`GETTING_STARTED.md`](GETTING_STARTED.md) — add a "Derived outputs" section with load snippets if jamboree participants will use them.
- [`synapse_paths.tsv`](synapse_paths.tsv) — add a new column only if the derivatives live in their own Synapse folders (vs. inside the existing per-output bundles).
- **Google Sheet + agent-ready docs** (Step 7 of [`TODO.md`](TODO.md)): friendly view of `synapse_paths.tsv` for jamboree participants + skills/docs so they can explore data and run analyses with agents. Link the planning doc to the sheet.
- **Push pending commits to GitHub** before sharing with the teammate / jamboree — keep `origin/main` in sync.

Gated on the derived-files decision completing.

### Mirror Gersbach Hep cNMF outputs to canonical Synapse layout &nbsp;·&nbsp; **High**
Sara likely has cNMF runs for Gersbach Hep already; we need them shaped to our schema (selected-k bundle + sweep-as-provenance per [`schemas/cnmf.json`](schemas/cnmf.json)) and uploaded under `2026_UTSW/datasets/Gersbach_WTC11-hepatocyte-differentiation_TF-Perturb-seq/cnmf/`.

- Ping Sara directly with: schema link, target Synapse parent folder, mirror tooling pointer (`scripts/mirror_cnmf_outputs.py`).
- Full context (CRISPR + e-dist also pending from Sara, but cNMF is the priority pull here) in [`issues/gersbach-hep-deliverables.md`](issues/gersbach-hep-deliverables.md).
- While we have her on the line, also touch on the CRISPR + e-distance bundles she owes — same Issue doc.

### Compare Huangfu datasets to existing pipeline outputs &nbsp;·&nbsp; **TBD**
Compare Huangfu DE/ESC CRISPR pipeline outputs to existing reference pipeline outputs. Context (paths, reference runs, specific comparisons, priority) to be filled in.

### Get Engreitz endothelial data through the CRISPR pipeline &nbsp;·&nbsp; **Low**
End-to-end: get raw data uploaded to the IGVF portal → run CRISPR pipeline (Stage 2 of [`docs/analysis/PIPELINES.md`](../../analysis/PIPELINES.md)) → mirror canonical 3-folder bundle to Synapse. Currently blocked at stage 1 (data not on the portal).

- Stage 1 (upload-to-portal sub-step) context in [`issues/engreitz-no-data.md`](issues/engreitz-no-data.md).
- Once stage 3 lands, downstream cNMF + e-distance for Engreitz unblock.
